name: market_pipe
version: 1.0.0
config-version: 2

profile: market_pipe

model-paths:
  - models
target-path: target
packages-install-path: dbt_packages
clean-targets:
  - target
  - dbt_packages

models:
  market_pipe:
    +materialized: view
    staging:
      +schema: staging
    marts:
      +schema: marts

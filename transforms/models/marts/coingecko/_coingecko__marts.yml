version: 2

models:
  - name: mart__coins_list
    columns:
      - name: coin_id
        tests:
          - not_null
          - unique
  - name: mart__asset_platforms_list
    columns:
      - name: asset_platform_id
        tests:
          - not_null
          - unique
